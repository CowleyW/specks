package main

import "errors"

func parse(sql string) (Table, error) {
	return Table{}, errors.New("not yet implemented")
}

func parseStatement(sql string) (Table, error) {
	return Table{}, errors.New("not yet implemented")
}
